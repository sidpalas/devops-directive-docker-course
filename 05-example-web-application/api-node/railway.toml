[build]
builder = "DOCKERFILE"
dockerfilePath = "/05-example-web-application/api-node/Dockerfile.railway"

[deploy]
restartPolicyMaxRetries = 10
restartPolicyType = "ON_FAILURE"

# Also set env vars:
# NODE_ENV=production
# PORT=3000
