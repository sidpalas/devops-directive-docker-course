[build]
builder = "DOCKERFILE"
dockerfilePath = "/05-example-web-application/api-golang/Dockerfile.railway"

[deploy]
restartPolicyMaxRetries = 10
restartPolicyType = "ON_FAILURE"

# Also set env vars:
# PORT=8080
